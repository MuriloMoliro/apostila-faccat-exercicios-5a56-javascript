// Escreva um algoritmo para ler as dimensões de um retângulo (base e altura), calcular e escrever a
// área do retângulo.

let base = parseInt(prompt("Digite o valor da base: "))
let altura = parseInt(prompt("Digite o valor da altura: "))

let area = (base * altura) / 2

alert("O valor da area é de: " + area)